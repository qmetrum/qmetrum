# PDF Report Audit — 2026-07-26

77 confirmed fabrications (44 client-facing) across the 5 report templates,
router, styles, helpers, and frontend consumers. Verified adversarially.

## Confirmed findings

- **[client-facing-fabrication]** `reports/report_router.py:514` — Quarterly scenario table: 4 hardcoded scenarios with invented 12-month returns (+7/-9/-23/+18%), drawdowns, and probabilities (55/25/12/8%) rendered as a chart + table on the 'SCENARIO ANALYSIS' page and multiplied into dollar 'Portfolio Impact' (template_quarterly L365-377). Identical for every client and every portfolio.
- **[client-facing-fabrication]** `reports/report_router.py:763` — Rebalancing scenario stress test: 4 hardcoded scenarios with invented current_return/proposed_return pairs (e.g. Severe downturn -23%/-18%) rendered as the 'SCENARIO STRESS TEST' butterfly chart claiming the proposal improves stress outcomes — the improvement deltas are invented, not computed from either portfolio.
- **[client-facing-fabrication]** `reports/report_router.py:487` — Quarterly portfolio_value fallback: sum(last_price * weight * 100000) — a synthetic ~$100k-per-unit-weight AUM printed on the cover ('PORTFOLIO VALUE'), the exec-summary metric card, per-holding dollar values (L505), and multiplied into scenario dollar impacts.
- **[client-facing-fabrication]** `reports/report_router.py:854` — Year-end model_accuracy: hit_rate=0.72 and avg_error=0.034 hardcoded; rendered as 'Directional hit rate 72%' / 'Average forecast error 3.4%' metric cards and narrated as fact in the 'How accurate were our forecasts?' paragraph (template_yearend L239-252). Only best_model is real (model_used).
- **[client-facing-fabrication]** `reports/report_router.py:625` — Market-event 'event window': portfolio series is the LAST 30 days of the 2y history (portfolio_prices[-30:]) regardless of event_date, plotted against fabricated consecutive calendar dates event_date-15..+14 (L629, includes weekends). portfolio_return_event (L649) = return since index 14 of that series, i.e. NOT the event impact unless the event happens to be ~15 trading days ago. Benchmark IS fetched for the true event window (L631-637) so the two lines on one chart can cover different periods.
- **[client-facing-fabrication]** `reports/report_router.py:643` — Benchmark aligned to portfolio by array position, not date; when shorter it is edge-padded (np.pad mode='edge'), fabricating a flat benchmark tail in the event chart and drawdown comparison.
- **[client-facing-fabrication]** `reports/report_router.py:668` — Market-event forward_outlook: hardcoded sentence 'Our ensemble model projects a gradual recovery over the next 90 days' — printed under 'What we expect from here' regardless of what forecast_result actually projects (and the fetch used horizon_days=30, not 90).
- **[client-facing-fabrication]** `reports/report_router.py:673` — Market-event action_items: canned advice ('If fragility exceeds 2.0 for 5+ sessions, reduce equity exposure by 5%', 'Schedule client call') presented as the advisor's numbered action plan for this specific event.
- **[client-facing-fabrication]** `reports/report_router.py:727` — Rebalancing trades: shares = int(value / 100) ('rough estimate', also L731) — assumes every security trades at exactly $100; printed in the 'Shares' column of the PROPOSED TRADES table (template_rebalancing L179-186).
- **[client-facing-fabrication]** `reports/report_router.py:595` — Onboarding recommended_allocation = sector_map (identical to current_allocation; comment admits 'in production, this would come from an optimizer'). The PDF renders a side-by-side 'Current vs. recommended allocation' chart where the recommendation is a copy of the status quo dressed in different colors.
- **[client-facing-fabrication]** `reports/report_router.py:840` — Year-end portfolio_value_start fallback 2,000,000 (and value_end = 2M*(1+ytd) at L841); cover page prints a derived dollar 'P&L' and 'ENDING VALUE' from this synthetic base.
- **[client-facing-fabrication]** `reports/report_router.py:861` — Year-end year_ahead_themes: canned bullets including 'Maintain inflation protection through TIPS and commodity allocations' — references holdings the portfolio may not contain; rendered under '<year+1> OUTLOOK — Themes and positioning'.
- **[client-facing-fabrication]** `reports/report_router.py:542` — Onboarding portfolio_value fallback 1,000,000 (same at L657 market-event, L705 rebalancing) — printed on covers and used for per-holding dollar values and trade sizing.
- **[client-facing-fabrication]** `reports/report_router.py:168` — Cache match ignores weights: direct portfolio_id lookup requires only requested_tickers.issubset(cached_tickers) (a 2-ticker request can be served the full-portfolio blob); ad-hoc path (L184) requires ticker-set equality but never compares weights — a 90/10 request can be served a cached 50/50 portfolio's forecast, risk metrics, and price history. Ad-hoc scan is also not user-scoped (L176-179).
- **[client-facing-fabrication]** `reports/template_quarterly.py:232` — vol_hist = |convolve(returns, ones(21)/21)| * sqrt(252) * 100 — this is a smoothed MEAN return, not a standard deviation; it is not volatility at all, yet the chart legend labels it 'Historical (GARCH)' (L81). Real GARCH sigma_hist from the R service is available in risk_data and unused.
- **[client-facing-fabrication]** `reports/template_quarterly.py:238` — 'Volatility cone' = vol_fc * 1.4 / * 0.6 — an invented ±40% band presented as a GARCH cone; the R service returns a real MC vol cone (sigma_mc_lower/median/upper) that is never read.
- **[client-facing-fabrication]** `reports/template_quarterly.py:355` — Scenario page prose: 'Probabilities are derived from current regime indicators, yield curve shape, and credit spread analysis.' No yield-curve or credit-spread analysis exists anywhere in the codebase; the probabilities are the hardcoded constants from the router.
- **[client-facing-fabrication]** `reports/template_quarterly.py:403` — Methodology claims the tournament runs '(ARIMA, LSTM, Prophet, BSTS, Quantum Kernel SVR)' — the actual fifth family is QRC (QuantumReservoirForecaster, forecasting_logic.py L27, L873-888); the exec summary (L269) lists only four. Also L422 describes the 'severe downturn scenario uses a 2x volatility scaling' — describing a scenario that is a hardcoded table row, not a simulation.
- **[client-facing-fabrication]** `reports/report_styles.py:261` — standard_disclaimer hardcodes '{firm_name} is a registered investment adviser.' into every PDF — a regulatory status claim the engine cannot know; false for any non-RIA user of the product.
- **[placeholder]** `reports/template_quarterly.py:195` — Chart x-axes are synthetic calendar days counted back from report_date (also L206, dates_fc L196); the real trading dates are passed as 'portfolio_dates' (router L513) but read into a variable at L200 and never used — 252 trading days get compressed onto ~8.3 calendar months, so every historical date label on the forecast/volatility/drawdown charts is wrong.
- **[placeholder]** `reports/template_quarterly.py:228` — Missing-CI fallback: fc_lower/fc_upper = fc_prices * 0.92 / 1.08 — an invented '95% confidence' band (legend at L63-64); missing-forecast fallback extrapolates flat drift (L220-222) and missing-history fallback draws a flat 100.0 line (L209), all rendered indistinguishably from model output.
- **[placeholder]** `reports/report_router.py:364` — risk_metrics defaults: var_95_daily=-0.02, cvar_95_daily=-0.03 when the forecast engine returns nothing — printed in the quarterly 'Detailed risk metrics' table and market-event VaR field as if computed.
- **[placeholder]** `reports/report_router.py:597` — Canned boilerplate lists presented as advisor guidance: onboarding next_steps (L597-601), rebalancing implementation_notes (L787-791), year-end proposed_changes (L866-868).
- **[placeholder]** `reports/report_router.py:885` — Portfolio-linked quarterly defaults advisor_name='Financial Advisor', firm_name='Advisory Firm' (L886) — placeholder identities printed on the cover, headers, and disclaimer of a client deliverable. Also client_name becomes the portfolio name (L915).
- **[placeholder]** `reports/report_data_helpers.py:117` — Silent zero/neutral fallbacks rendered as data: months with no prices show 0.0% return (L117), 0.0 vol (L198), fragility 1.0 (L246), beta 1.0 (L267/L273); benchmark missing collapses to a single flat point (L295). Year-end charts draw these as real observations with no 'data unavailable' annotation.
- **[cosmetic]** `reports/report_router.py:844` — Year-end sharpe/sortino/max_drawdown/annualized_vol come from _fetch_portfolio_data's trailing-2y window (L810), not the review_year — a '2025 in review' report labels 2024-2026-window statistics as the year's risk metrics.
- **[cosmetic]** `reports/report_router.py:503` — 'Sector' column filled from fundamentals 'type' with default 'Equity' (also L548, L588) — asset type masquerading as sector classification in holdings tables and the onboarding allocation chart.
- **[cosmetic]** `reports/report_router.py:300` — qmc_result (simulate_trained_vqmc, 5000 sims) is computed on every uncached report run and stored in the nightly blob, but no template ever renders it — pure wasted compute today; conversely the scenario 'color' field the router passes (L515-518) is ignored by _make_scenario_chart which recomputes colors (template_quarterly L99-100).
- **[cosmetic]** `reports/template_quarterly.py:438` — Footer meta hardcodes 'Engine version: Forecastium v2.1 | Models: 5-family ensemble + GARCH(1,1)-std'; cover footer hardcodes 'Powered by Forecastium Risk Intelligence Engine' (report_styles.py L214) — invented product branding/version in a project named qmetrum.
- **[client-facing-fabrication]** `reports/report_router.py:514` — The entire 'scenarios' list is hardcoded: Base case +7%/55% prob, Mild recession -9%/25%, Severe downturn -23%/12%, Strong recovery +18%/8%, with invented per-scenario drawdowns. Identical for every portfolio, client, and date. Surfaces as the whole SCENARIO ANALYSIS page: bar chart with '(X% prob.)' labels, and the 12M Return / Max Drawdown / Portfolio Impact ($) / Probability table. The 'color' field (TEAL/AMBER/CORAL/BLUE) is dead — the chart recomputes colors from return sign (template_quarterly.py:99).
- **[client-facing-fabrication]** `reports/template_quarterly.py:101` — _make_scenario_chart reads s['prob'] and prints ' +X.X%  (NN% prob.)' next to each bar (lines 101, 109) — rendering the fabricated probabilities as chart annotations in the flagship PDF.
- **[client-facing-fabrication]** `reports/template_quarterly.py:365` — Scenario detail table (lines 365-377) renders fabricated '12M Return', 'Max Drawdown', 'Probability', and a dollar 'Portfolio Impact' = portfolio_value × fabricated return (line 368) — invented dollar losses/gains presented to the client as analysis.
- **[client-facing-fabrication]** `reports/template_quarterly.py:353` — Body text: 'Probabilities are derived from current regime indicators, yield curve shape, and credit spread analysis.' No yield-curve or credit-spread code exists anywhere in the codebase (grep confirms the only occurrence of those phrases is this template line 356). Probabilities are constants.
- **[client-facing-fabrication]** `reports/template_quarterly.py:421` — Methodology: 'The severe downturn scenario uses a 2x volatility scaling to stress-test tail outcomes.' No 2x scaling exists anywhere; the engine's builtin high_vol_regime uses 1.25x (portfolio_logic.py:23) and the fabricated 'Severe downturn' dict has no volatility knob at all.
- **[client-facing-fabrication]** `reports/template_quarterly.py:420` — Methodology: 'Probabilities are informed by current regime classification, not predicted.' False — the probabilities are hardcoded constants in report_router.py:515-518; the engine emits no scenario probabilities at all.
- **[client-facing-fabrication]** `reports/template_quarterly.py:188` — Template reads fc['forecast_confidence_interval'] but HybridForecaster.predict() emits 'upper_ci'/'lower_ci' (forecasting_logic.py:1756-1757); the key never exists in live or cached data, so the fallback fc_prices*0.92/*1.08 (lines 227-229) always fires. The forecast chart's shaded band labeled '95% confidence' (line 64) is a fixed ±8% envelope, and the caption (lines 293-299) describes a bootstrap/GARCH band that is not what is drawn.
- **[client-facing-fabrication]** `reports/template_quarterly.py:232` — vol_hist = |21-day moving AVERAGE of returns|·√252·100 — this is annualized drift magnitude, not volatility (should be a rolling std at minimum). It is plotted with legend 'Historical (GARCH)' (line 81). Real GARCH conditional vol exists in risk_data['sigma_hist'] (risk_service_r/app/api.R:52,101) and is even listed in the template's own docstring (line 165) but never used. The historical line is wrong in both magnitude and meaning.
- **[client-facing-fabrication]** `reports/template_quarterly.py:238` — Volatility cone = vol_fc*1.4 / vol_fc*0.6 — arbitrary hardcoded multipliers rendered with legend 'Volatility cone' under the title 'Annualized volatility with GARCH cone'. The real simulated vol cone (sigma_mc_lower/median/upper, api.R:93-97,111-113) is returned by the risk service and ignored.
- **[client-facing-fabrication]** `reports/template_quarterly.py:270` — Executive summary: 'All projections include 95% confidence intervals derived from Monte Carlo simulation.' The rendered price band is the ±8% fallback (see line 188 finding); even the real R MC price cone is 5th/95th percentile = a 90% interval (api.R:87-89), and the bootstrap band that predict() actually emits (2.5/97.5, forecasting_logic.py:1000-1001) never reaches this template.
- **[client-facing-fabrication]** `reports/template_quarterly.py:403` — Methodology names the fifth tournament family as 'Quantum Kernel SVR'. The actual fifth family is QRC — Quantum Reservoir Computing (forecasting_logic.py:27,179-180; walk-forward loop at 872-886; scores dict 'qrc' at 700). A quantum_kernel module exists but only for regime labeling (forecasting_logic.py:554), not as a forecast family.
- **[client-facing-fabrication]** `reports/template_quarterly.py:429` — Methodology: CI paragraph claims 'scenario-consistent bands'. Nothing links the forecast CI to scenarios; scenario-consistent bands are the MPS fans (portfolio_logic.py:383-426), which this report does not use. The bootstrap-scaled-by-GARCH description (lines 425-428) does match _build_stochastic_representative_path (forecasting_logic.py:947-1002) but omits the 0.70 shock damping (line 989) and the [0.5,2.5] vol-scale clip (line 988) — and that band is not the one drawn (see ±8% finding).
- **[client-facing-fabrication]** `reports/report_router.py:486` — Default portfolio_value = last_price × weight × 100000 — an arbitrary notional invented when the caller omits portfolio_value. It becomes the cover 'PORTFOLIO VALUE' metric (template line 249), the 'Total value' card (277), per-holding dollar values (router 505, template 390-392), and multiplies the fabricated scenario returns into dollar 'Portfolio Impact' figures (template 368).
- **[placeholder]** `reports/template_quarterly.py:327` — Risk table renders canned defaults when metrics are missing: VaR -2.00% and CVaR -3.00% (rm.get defaults, lines 327-329; same defaults injected upstream at report_router.py:364-365) — a failed risk-service call silently prints plausible-looking risk numbers.
- **[placeholder]** `reports/template_quarterly.py:237` — When sigma_fc_mean is missing, the 'Forecast (mean)' volatility line is np.linspace(vol_hist[-1], vol_hist[-1]*0.95, horizon) — a fabricated gentle decay presented as the GARCH mean forecast.
- **[cosmetic]** `reports/template_quarterly.py:195` — dates_hist/dates_fc are synthetic calendar-day sequences (report_date − N days; lines 195-196, 206), while the real trading dates passed by the router ('portfolio_dates', read at line 200, router 513) are never used. ~252 trading days (~12 months) are compressed onto ~8.3 calendar months, so every x-axis date label on the forecast, volatility, and drawdown charts is wrong by up to ~4 months.
- **[cosmetic]** `reports/template_quarterly.py:338` — Risk table interprets Maximum Drawdown as 'Largest peak-to-trough decline in trailing 12 months', but max_drawdown is computed over the full ~2y training series (forecasting_logic.py:363-365; router fetches period='2y' at report_router.py:244).
- **[cosmetic]** `reports/template_quarterly.py:268` — Executive summary says 'multi-model ensemble (ARIMA, LSTM, Prophet, BSTS)' — omits QRC and mislabels a winner-take-all tournament as an 'ensemble' (the forecast caption at 294-295 correctly says 'winning model'). Also omits the regime-conditional winner override (forecasting_logic.py:1094-1120) that the '50/50 weighting' sentence at line 407 glosses over.
- **[cosmetic]** `reports/template_quarterly.py:59` — Forecast chart title hardcodes 'Portfolio forecast — 90-day horizon' while horizon_days is caller-configurable; the subheading (line 288) is correctly dynamic. Similarly the scenario chart title says 'projected 12-month outcomes' (line 95) and the table column '12M Return' (365) though no 12-month simulation exists (engine scenario fans run horizon_days, default 90).
- **[cosmetic]** `reports/template_quarterly.py:438` — Footer meta: 'Engine version: Forecastium v2.1' — invented product name and version; 'Forecastium' exists only in report copy (also report_styles.py:214 'Powered by Forecastium Risk Intelligence Engine'). No such version identifier exists in the service.
- **[client-facing-fabrication]** `reports/report_router.py:763` — (Adjacent, same honesty pass) Rebalancing report 'scenarios' are hardcoded current/proposed return pairs (Base +7%/+6.5%, Recession -9%/-6.5%, Severe -23%/-18%, Rate spike -5.5%/-3.2%) rendered as a butterfly chart claiming scenario-engine output (template_rebalancing.py:73-104).
- **[client-facing-fabrication]** `reports/report_router.py:854` — (Adjacent) Year-end report 'model_accuracy' is hardcoded: hit_rate 0.72, avg_error 0.034 — invented model-performance claims; real validation scores exist in data['model_validation'] (forecasting_logic.py:1085-1092).
- **[client-facing-fabrication]** `reports/report_router.py:669` — (Adjacent) Market-event report 'forward_outlook' is canned: 'Our ensemble model projects a gradual recovery over the next 90 days' regardless of what the forecast says, and 'action_items' (673-677) are fixed advice including 'reduce equity exposure by 5%'.
- **[placeholder]** `reports/report_router.py:595` — (Adjacent) Onboarding report 'recommended_allocation' is a copy of the current allocation — the code comment admits 'in production, this would come from an optimizer'.
- **[client-facing-fabrication]** `reports/template_rebalancing.py:164` — Callout unconditionally claims the rebalance 'reduces annualized volatility ... while improving the Sharpe ratio' using abs(vol_reduction) — if the proposal RAISES vol or LOWERS Sharpe the PDF still asserts improvement (lines 162-167); chart title at line 101 ('improvement from rebalance') presupposes the same
- **[client-facing-fabrication]** `reports/report_router.py:629` — Fabricated x-axis dates: [event_date-15d+i for i in range(len(prices))] paints calendar dates centered on the event onto whatever recent trading-day series was grabbed at line 625; both event chart and drawdown chart carry these invented dates
- **[client-facing-fabrication]** `reports/report_router.py:649` — Cover 'EVENT IMPACT' and the outperformance callout computed from hardcoded index 14 (port_idx[-1]/port_idx[14]) — index 14 is where event_date sits in the FABRICATED date array, not in the real series (lines 649-650; surfaces at template_market_event.py:122-127, 143-166)
- **[client-facing-fabrication]** `reports/template_yearend.py:250` — Canned interpretive claim 'reflecting the market's trend-driven character this year' asserted regardless of which model actually won the walk-forward tournament (lines 245-252)
- **[placeholder]** `reports/report_router.py:787` — implementation_notes canned 3-step generic plan (787-791) rendered as the numbered 'IMPLEMENTATION PLAN' section of every rebalancing PDF
- **[cosmetic]** `reports/template_rebalancing.py:48` — Bar-label formatter treats any value >1 as a percentage — a Sharpe ratio of 1.2 prints as '1.2%' in the current-vs-proposed risk chart
- **[cosmetic]** `reports/template_onboarding.py:88` — set_xlim(0,55) clips allocation bars above 55% and pushes their value labels off-canvas (the module's own demo has 65% US Equity, which clips); gauge chart similarly hard-caps at 35% vol (line 53) so higher actual vol plots off-chart
- **[cosmetic]** `reports/template_yearend.py:187` — 'Alpha generated' metric card is a simple return difference (portfolio minus benchmark, line 183), not alpha — mislabeled statistic
- **[cosmetic]** `reports/template_market_event.py:27` — Benchmark hardcoded as 'S&P 500' in chart legends and metric labels (also line 60; template_yearend.py:27,186; template_market_event.py:147) while the data layer silently falls back to SPY/VOO (report_data_helpers.py:28,46) — label may not match the series plotted
- **[cosmetic]** `reports/report_styles.py:214` — Cover footer 'Powered by Forecastium Risk Intelligence Engine' — hardcoded product brand in a project named qmetrum; verify intended branding before client distribution
- **[cosmetic]** `reports/template_market_event.py:90` — Dead expected fields: portfolio_value (90), portfolio_drawdown_peak (93), var_95 (96) documented and dutifully computed/passed by the router (report_router.py:657,663,666) but never rendered anywhere in the PDF; likewise sortino in template_rebalancing.py:119 and sharpe in template_onboarding.py:124
- **[cosmetic]** `reports/template_yearend.py:100` — 'Fragile threshold' drawn at 1.5 in the risk-evolution chart while the market-event action items cite 2.0 as the trigger (report_router.py:675) — inconsistent risk thresholds across client documents
- **[cosmetic]** `reports/template_onboarding.py:228` — __main__ demo blocks in all four templates (onboarding 228-271, rebalancing 214-258, market_event 218-279, yearend 274-326) contain fictional clients ('Richardson Family Trust', 'Meridian Wealth Advisors') and write to nonexistent /home/claude/reports — dev leftovers, not reachable via the API but risky if ever executed as scripts
- **[client-facing-fabrication]** `clients/page.tsx:92` — Onboarding report hardcodes risk_tolerance: "Moderate" and target_vol: 0.10 for every client with no input UI — the PDF's risk-alignment verdict ('stated tolerance vs actual volatility') is built on a stated tolerance that was never collected from anyone.
- **[client-facing-fabrication]** `clients/page.tsx:97` — Market-event briefing invents the event: event_name "Market Event", event_date = today, event_summary "Market turbulence event analysis." (lines 95-100). The client receives a PDF briefing about a market event that does not exist.
- **[client-facing-fabrication]** `clients/page.tsx:106` — Rebalancing proposal sends proposed_assets identical to current_assets plus canned rationale "Periodic rebalancing review." — the PDF's before/after risk comparison and trade list are a proposal with zero actual changes and boilerplate justification.
- **[client-facing-fabrication]** `reports/page.tsx:117` — Even on the full Report Center form, rebalancing sends proposed_assets: assets (same array as current_assets, lines 117-118) — there is no UI to enter a proposed allocation, so every rebalancing PDF's before/after comparison is vacuous regardless of what the advisor types in the rationale box.
- **[client-facing-fabrication]** `scenarios/page.tsx:243` — "Export to PDF" after a scenario run sends only {client_name, advisor_name, firm_name, assets, portfolio_value} (lines 243-249) — the fanResults, scenario specs, and dollar-impact numbers displayed on screen are NOT in the payload, so the exported quarterly PDF's scenario section cannot match what the advisor just showed the client.
- **[placeholder]** `reports/page.tsx:122` — Year-end payload spreads base which carries portfolio_value, but YearEndReportRequest (api.ts:890-897) expects portfolio_value_start/portfolio_value_end — the user-typed value is silently dropped (spread bypasses excess-property checks) and start/end values are never collected, so any dollar figures in the year-end PDF are backend-invented or missing.
- **[placeholder]** `scenarios/page.tsx:77` — portfolioValue defaults to 1_000_000; if the advisor never edits it, the exported quarterly PDF states a $1,000,000 portfolio value that was never entered (it also feeds the on-screen Dollar Impact tiles at lines 279/292).
- **[placeholder]** `providers/BrandingProvider.tsx:17` — Branding defaults firmName: "Advisory Firm" / advisorName: "Financial Advisor" (lines 17-18) flow directly into clients-page report payloads (clients/page.tsx:82-83) with no edit opportunity at generation time — PDFs go out signed by placeholder identities if branding was never configured.
- **[placeholder]** `clients/page.tsx:78` — weight ?? 0 coercion (also reports/page.tsx:84): portfolios saved without weights produce an all-zero holdings table in every report type; no portfolio_value is passed at all from the clients page (base at lines 80-85 omits it), so dollar figures in those PDFs are unanchored.
- **[cosmetic]** `reports/page.tsx:17` — Report-type card copy promises "ensemble forecast, GARCH volatility, scenario analysis" (line 17) and "model accuracy, year-ahead themes" (line 45) — with a names+weights-only payload, all of that content is generated server-side with zero grounding from anything the advisor has seen or run, setting the honesty bar the backend must actually meet.
- **[cosmetic]** `reports/page.tsx:129` — catch { /* handle silently */ } (same at clients/page.tsx:114-115): a failed PDF generation gives the advisor no feedback — spinner stops, nothing downloads, no error; on /scenarios exportPdf has no catch at all (unhandled rejection) and no loading state (button at lines 518-524 is never disabled while generating).

## Fix plan (all five reports — user decision 2026-07-26)

- **A. Shared**: cache match must respect weights; never invent portfolio values (omit $ when unknown); neutral/configurable legal line (kill 'registered investment adviser' + 'Forecastium' branding); no silent zero-fallbacks.
- **B. Quarterly**: real MPS scenario section + AI analysis (scenario_explainer); fix CI key mismatch (upper_ci/lower_ci) + drop ±8% fallback; real GARCH sigma + MC cone (risk_data) replacing fake vol line + ±40% cone; real dates on axes; rewrite methodology text to match engine (QRC, no probability claims, no 2x-scaling claim).
- **C. Rebalancing**: real proposed weights (engine target_weights + rebalancing.suggestions); computed current-vs-proposed metrics + MPS stress; real share prices or omit; conditional callout.
- **D. Market-event**: slice by real event_date with real dates; date-aligned benchmark; real event impact; outlook from actual forecast (AI, disclaimed); real event inputs from UI.
- **E. Year-end**: real walk-forward model accuracy; real values or omit; AI year-ahead grounded + disclaimed; fix alpha label.
- **F. Onboarding**: reframe recommendation honestly (rebalancing suggestions or current-only); UI inputs for risk tolerance.
- **G. Frontend**: send real values/run data; no silent catches; disable buttons lacking real inputs.

## Status 2026-07-27: phases A-F DONE (all five templates honest + narrative layer)

All five reports now: real data or honest omission, AI narrative layer
(report_narrator agents, grounded + disclaimed, options-to-evaluate framing),
zero em/en dashes. 77 report tests passing; all five PDFs build.

Open residuals (small):
- Portfolio-linked quarterly endpoint still defaults advisor_name/firm_name to
  placeholders via Query defaults (report_router.py ~L1442).
- report_data_helpers.py ~L262: months with no vol snapshots padded with
  fragility 1.0 drawn as real observations in the year-end risk chart.
- Rebalancing before/after chart draws a 0.0 bar when VaR is unavailable
  (upstream now passes None; chart could annotate 'n/a' instead).

## Phase G (frontend) still open
- reports/clients pages send proposed_assets == current_assets (rebalancing
  needs a proposed-allocation input; PDF now honestly says "essentially
  unchanged").
- Market-event UI must supply a real event_name/event_date (backend now 400s
  on dates outside history instead of faking a window).
- Onboarding risk_tolerance/target_vol now REQUIRED (both pages already send
  them); clients page still hardcodes Moderate/0.10 with no input UI.
- Year-end: portfolio_value_start optional (omits dollars when absent); new
  optional proposed_changes field has no UI; endpoint 400s when review_year
  has no price data.
- Silent catch {} around PDF downloads; scenarios/quarterly should pass the
  run's scenarios (done for scenarios page); portfolioValue default 1M input.


## Phase G DONE (2026-07-27)
- Clients page: fabricated one-click report payloads replaced by links to the
  Report Center preconfigured via /reports?portfolio=..&type=..
- Report Center: proposed-allocation editor for rebalancing (seeded from
  current, live total, normalization + "essentially unchanged" notices), tax
  considerations input, year-end start/end value fields (optional, honest
  omission), per-type required-input gating ("reports never invent missing
  data"), error banner surfacing backend refusals (Blob-aware detail parsing).
Remaining nits: portfolio-value input still displayed for year-end though
unused; portfolio-linked quarterly Query placeholder defaults; fragility 1.0
padding in year-end risk chart; rebalancing chart 0.0 bar when VaR is None.
